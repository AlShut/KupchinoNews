<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.example.helen.kupchinonews">

    <application  android:label="@string/app_name"
        android:icon="@drawable/logo"
        android:theme="@style/DefaultMaterialTheme">
        <activity android:name=".Main"
            android:label="Новости Купчино">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".Feed"
            android:label="Новостная лента">
        </activity>

        <activity android:name=".ExtendedMail"
            android:label="Внешняя e-mail библиотека">
        </activity>

        <activity android:name=".ExtendedMailVideoSelect"
            android:label="Выбрать видео для отправки">
        </activity>

        <activity android:name=".Chat"
            android:label="Чат">
        </activity>

    </application>

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.MEDIA_CONTENT_CONTROL"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

</manifest>